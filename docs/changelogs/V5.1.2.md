# REISHI v5.1.2

## 變更摘要

- **版本號**：v5.1 → v5.1.2  
- **數據管理**：下載續跑與路徑除錯  
  - 下載時呼叫 `_resolve_data_roots()`，確保與當前工作目錄一致  
  - 新增運行證據日誌（`[REISHI repair_start]`、`write_partial_ok`/`write_partial_fail`、`resume_load_ok`/`resume_load_fail`），方便排查續跑不生效  
  - 除錯 log 寫入 `.cursor/debug.log` 與 `debug_run.log`，關鍵 tag 同時列印到 stdout  

---

*發布日期：2025-01-30*
