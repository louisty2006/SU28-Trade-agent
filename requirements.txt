# REISHI 霊視 v5.0 MVP 依賴套件

# 核心數據處理
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0   # Parquet 讀寫（數據管理 K 線存檔／續跑）

# 股票數據
yfinance>=0.2.28

# API 請求
requests>=2.31.0

# 環境變數管理
python-dotenv>=1.0.0

# Finnhub API
finnhub-python>=2.4.0

# 回測：美國市場交易日曆
pandas_market_calendars>=4.3.0

# v5.0 新增依賴
pyyaml>=6.0  # 配置文件管理
mplfinance>=0.12.10b0  # K線圖生成
matplotlib>=3.7.0  # 圖表繪製
pillow>=10.0.0  # 圖像處理

# LLM API (按需安裝)
# openai>=1.0.0          # OpenAI GPT
# anthropic>=0.8.0       # Anthropic Claude
